import React from "react";
import Navbar from "../Navbar/Navbar";
import "./Header.css";

function Header({ heading }) {
  return (
    <header>
      <Navbar />
    </header>
  );
}

export default Header;
